# LexiAid Environment Configuration
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# Database Configuration - ALL FIELDS REQUIRED
# No fallback values - application will fail if these are not set
DB_HOST=localhost
DB_USER=your_database_user
DB_PASS=your_secure_database_password
DB_NAME=lexiaid_db

# Admin Credentials - REQUIRED for diagnostic access
# Change these from defaults before deployment
ADMIN_USER=admin
ADMIN_PASS=your_secure_admin_password

# Environment - Set to 'production' for deployment
APP_ENV=development

# SECURITY NOTES:
# - Never use empty passwords in production
# - Use strong, unique passwords for all accounts
# - Keep this file outside web-accessible directories
# - Validate configuration with validate_env.php before deployment
